spring:
  application:
    name: springboot-department-docker
  datasource:
    url: jdbc:mysql://localhost:3306/deptdb
    username: root
    password: root
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 9091

